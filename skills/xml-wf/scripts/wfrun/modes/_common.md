- DEFINE:
  - mode-output=the mode's native output (report/plan/design/review/debug/execute-work)
  - target-artifact=project end product (production code/applied patch/prod content/assets)
- Deliverables: files at paths the task names are this step's mode-output, NOT
  target-artifacts — writing them is always allowed, whatever the mode. (In
  `execute`, editing target sources named by the task is itself the work.)
- MUST: produce mode-output; write UNKNOWN/TBD for missing facts — never invent them.
- NEVER: exceed the mode's remit, switch modes silently.
- DO: cite evidence for factual claims.
